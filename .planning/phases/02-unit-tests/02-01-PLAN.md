---
phase: 02-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/loanCalculator.test.js
  - tests/unit/payloads.test.js
  - tests/unit/validators.test.js
autonomous: true
requirements:
  - UNIT-01
  - UNIT-02
  - UNIT-03
  - UNIT-04

must_haves:
  truths:
    - "calculatePayments returns correct monthlyPayment, totalInterestPaid, totalPaid, loanAmount for known inputs"
    - "calculatePayments returns error object when vehiclePrice is 0 or falsy"
    - "calculatePayments defaults loanTerm to 72 and interestRate to 11.33 when not provided"
    - "buildPreApprovalPayload trims all string fields and copies answers"
    - "All 16 validator exports return null for valid input and correct error string for invalid input"
    - "updateItem writes to sessionStorage and reads back the value"
    - "updateAmount returns its input unchanged"
  artifacts:
    - path: "tests/unit/loanCalculator.test.js"
      provides: "calculatePayments and formatCurrency tests"
      min_lines: 40
    - path: "tests/unit/payloads.test.js"
      provides: "buildPreApprovalPayload tests"
      min_lines: 30
    - path: "tests/unit/validators.test.js"
      provides: "All 16 validator export tests"
      min_lines: 100
  key_links:
    - from: "tests/unit/loanCalculator.test.js"
      to: "src/utils/loanCalculator.js"
      via: "import { calculatePayments, formatCurrency }"
      pattern: "import.*calculatePayments.*from.*loanCalculator"
    - from: "tests/unit/payloads.test.js"
      to: "src/pages/loans/payloads.js"
      via: "import { buildPreApprovalPayload }"
      pattern: "import.*buildPreApprovalPayload.*from.*payloads"
    - from: "tests/unit/validators.test.js"
      to: "src/utils/validators.js"
      via: "import all 16 exports"
      pattern: "import.*validateForm5.*from.*validators"
---

<objective>
Write unit tests for all three pure utility modules: loanCalculator.js, payloads.js, and validators.js.

Purpose: Cover UNIT-01 through UNIT-04 — the pure function layer that has zero dependencies on React, context, or DOM. These are the fastest and simplest tests to write and run.
Output: 3 test files in tests/unit/ that all pass under `npm run test:unit`.
</objective>

<execution_context>
@/Users/forumgandhi/Desktop/atoloan-ui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forumgandhi/Desktop/atoloan-ui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-unit-tests/02-CONTEXT.md
@.planning/phases/02-unit-tests/02-RESEARCH.md

<interfaces>
<!-- Source functions under test — executor must read these files but here are the key signatures -->

From src/utils/loanCalculator.js:
```js
export function calculatePayments({ vehiclePrice, downPayment, loanTerm, interestRate })
// Returns: { monthlyPayment, totalInterestPaid, loanAmount, totalPaid, months }
// Or: { error: 'Please enter a vehicle price.' } when vehiclePrice is falsy
// Defaults: loanTerm || 72, downPayment || 0, interestRate || 11.33

export function formatCurrency(value)
// Returns: Number(value).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })
```

From src/pages/loans/payloads.js:
```js
export const buildPreApprovalPayload = ({ language, answers, otherMonthlyIncome, otherDownPayment, jobTitle, timeAtJobMonths, monthlyDebt, grossMonthlyIncome, zipCode, contactInfo, summary })
// Returns object with all string fields trimmed, contactInfo fields trimmed, answers shallow-copied
// sendPayload is NOT tested (per CONTEXT.md — out of scope for UNIT-03)
```

From src/utils/validators.js (16 named exports):
```
validateForm5, validateForm9, validateForm91, validateForm11, validateForm111,
validateForm13, validateForm14, validateForm15, validateForm15cosigner,
validateForm17, validateForm19, validateForm20, signupDemoForm,
validateFormHomepage, updateItem, updateAmount
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: loanCalculator.test.js and payloads.test.js</name>
  <files>tests/unit/loanCalculator.test.js, tests/unit/payloads.test.js</files>
  <read_first>
    - src/utils/loanCalculator.js (source under test — read formula logic)
    - src/pages/loans/payloads.js (source under test — read buildPreApprovalPayload shape)
    - tests/unit/setup.js (understand what matchers and cleanup are already registered)
    - vite.config.js (confirm globals: true, no explicit vitest imports needed)
  </read_first>
  <behavior>
    loanCalculator.test.js:
    - calculatePayments({vehiclePrice:25000, downPayment:3000, loanTerm:60, interestRate:6.5}) => months toBe(60), loanAmount toBeCloseTo(22000,2), monthlyPayment toBeCloseTo(430.16,2), totalInterestPaid toBeCloseTo(3809.60,2), totalPaid toBeCloseTo(25809.60,2) [UNIT-01, D-09, D-11]
    - calculatePayments({vehiclePrice:0, downPayment:0, loanTerm:60, interestRate:5}) => toEqual({error:'Please enter a vehicle price.'}) [UNIT-02]
    - calculatePayments({vehiclePrice:20000, downPayment:0}) => loanAmount toBeCloseTo(20000,2), months toBe(72) — tests zero down payment AND default term [UNIT-02]
    - calculatePayments({vehiclePrice:15000}) => interestRate defaults to 11.33, months defaults to 72 [UNIT-02]
    - formatCurrency(1234.5) => '1,234.50', formatCurrency(0) => '0.00'

    payloads.test.js:
    - buildPreApprovalPayload with whitespace-padded strings => all top-level strings trimmed [UNIT-03]
    - buildPreApprovalPayload contactInfo fields trimmed [UNIT-03]
    - buildPreApprovalPayload answers object is shallow-copied (not same reference) [UNIT-03]
    - buildPreApprovalPayload preserves language and summary as-is [UNIT-03]
  </behavior>
  <action>
    Create tests/unit/loanCalculator.test.js (per D-09, D-10, D-11):
    - Do NOT import describe/it/expect/vi from vitest — globals: true is set
    - import { calculatePayments, formatCurrency } from '../../src/utils/loanCalculator.js'
    - Use toBeCloseTo(expected, 2) for monthlyPayment, totalInterestPaid, totalPaid, loanAmount (per D-09, D-11)
    - Use exact toBe for months (integer, per D-11)
    - Test inputs: {vehiclePrice:25000, downPayment:3000, loanTerm:60, interestRate:6.5} => monthlyPayment~430.16, totalInterestPaid~3809.60, totalPaid~25809.60, loanAmount~22000, months===60
    - Edge: vehiclePrice=0 => {error:'Please enter a vehicle price.'}
    - Edge: downPayment=0 with vehiclePrice=20000 => loanAmount~20000, months===72 (default term)
    - Edge: no loanTerm, no interestRate => months===72 (default), uses 11.33 rate
    - formatCurrency(1234.5) => '1,234.50'; formatCurrency(0) => '0.00'

    Create tests/unit/payloads.test.js (per UNIT-03):
    - import { buildPreApprovalPayload } from '../../src/pages/loans/payloads.js'
    - Test with whitespace-padded inputs: otherMonthlyIncome:'  2500  ', otherDownPayment:'  1000  ', jobTitle:'  Engineer  ', monthlyDebt:'  500  ', grossMonthlyIncome:'  4000  ', zipCode:'  90210  '
    - contactInfo with padded fields: firstName:'  John  ', lastName:'  Doe  ', email:'  john@test.com  ', address:'  123 Main  ', city:'  LA  ', state:'  CA  ', zip:'  90210  ', phone:'  5551234567  '
    - Assert all trimmed: result.otherMonthlyIncome === '2500', result.jobTitle === 'Engineer', result.contactInfo.firstName === 'John', etc.
    - Assert result.answers is not the same reference: result.answers !== inputAnswers (use not.toBe)
    - Assert language and summary pass through unchanged
    - timeAtJobMonths passes through as-is (it's not a string that needs trimming in all cases)
  </action>
  <verify>
    <automated>cd /Users/forumgandhi/Desktop/atoloan-ui && npx vitest run tests/unit/loanCalculator.test.js tests/unit/payloads.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "toBeCloseTo" tests/unit/loanCalculator.test.js returns >= 4 (monthlyPayment, totalInterestPaid, totalPaid, loanAmount)
    - grep "toBe(60)" tests/unit/loanCalculator.test.js finds at least 1 match (months exact check)
    - grep "toBe(72)" tests/unit/loanCalculator.test.js finds at least 1 match (default term)
    - grep "error.*Please enter a vehicle price" tests/unit/loanCalculator.test.js finds 1 match
    - grep "buildPreApprovalPayload" tests/unit/payloads.test.js finds at least 2 matches (import + test)
    - grep "trim" tests/unit/payloads.test.js OR grep assertion checking trimmed values finds matches
    - npx vitest run tests/unit/loanCalculator.test.js exits 0
    - npx vitest run tests/unit/payloads.test.js exits 0
  </acceptance_criteria>
  <done>
    - calculatePayments happy path test passes with toBeCloseTo assertions at penny precision
    - calculatePayments edge cases (zero price, zero down, missing term, missing rate) all pass
    - formatCurrency returns expected formatted strings
    - buildPreApprovalPayload trims all string fields, shallow-copies answers, passes through language/summary
    - All tests green under npx vitest run
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validators.test.js — all 16 exports</name>
  <files>tests/unit/validators.test.js</files>
  <read_first>
    - src/utils/validators.js (source under test — all 16 exports, read every function's validation logic and exact error strings)
    - tests/unit/setup.js (confirm afterEach cleanup)
    - vite.config.js (confirm globals: true)
  </read_first>
  <behavior>
    For each of the 16 exports, test valid input => null AND invalid input => exact error string:
    - validateForm5: valid (all fields) => null; missing ssnoritin => 'Select Social Security Number or ITIN.'
    - validateForm9: valid => null; timeatjob='none' => 'You must select time at job.'
    - validateForm91: valid => null; timeatjobcosigner='none' => 'You must select time at job.'
    - validateForm11: valid => null; missing monthlyincome => 'Select your monthly income.'
    - validateForm111: () => null (D-02)
    - validateForm13: valid => null; no downpayment + no otherdownpayment => 'Enter a down payment amount.'
    - validateForm14: valid => null; monthlydebt='abc' => 'You must enter a number.'
    - validateForm15: valid => null; isRepoVisible=true + no howlongsincerepo => error
    - validateForm15cosigner: valid => null; isRepoVisible=true + no howlongsincerepocosigner => error
    - validateForm17: valid => null; no zipcode => 'You must enter your zip code.'
    - validateForm19: valid => null; no firstname => 'Enter Your First Name'
    - validateForm20: valid => null; no agreeauthorizedconsent => error
    - signupDemoForm: valid => null; no dealershipname => 'You must enter the name.'
    - validateFormHomepage: valid => null; no loantype => 'You must select loan type.'
    - updateItem: sessionStorage write+read => 'Lorem ipsum' (D-03)
    - updateAmount: (42) => 42 (D-04)
  </behavior>
  <action>
    Create tests/unit/validators.test.js covering ALL 16 exports (per D-01):
    - import { validateForm5, validateForm9, validateForm91, validateForm11, validateForm111, validateForm13, validateForm14, validateForm15, validateForm15cosigner, validateForm17, validateForm19, validateForm20, signupDemoForm, validateFormHomepage, updateItem, updateAmount } from '../../src/utils/validators.js'
    - Do NOT import describe/it/expect from vitest — globals: true
    - Use describe blocks per function for readability (discretion)

    For each validator function, write at minimum:
    1. A "returns null for valid input" test with all required fields populated
    2. An "returns error for invalid input" test with specific field(s) missing

    Exact error strings to assert (copy verbatim from source):

    validateForm5 valid input object:
    { ssnoritin: 'ss', employmenttype: '1', paymenttype: 'paystub', monthlyincome: '3000', timeatjob: '1', jobtitle: 'Dev', havedriverslicense: 'yes', downpayment: '1000', vin: 'ABC123' }
    Missing ssnoritin => 'Select Social Security Number or ITIN.'
    Missing employmenttype => 'Select Employment Type.'
    monthlyincome='8' + no othermonthlyincome => 'Select Monthly Income.'
    Exempt employment ('5') skips timeatjob/jobtitle checks
    downpayment='7' + no otherdownpayment => 'You must enter downpayment amount.'

    validateForm9: {timeatjob:'over2years', jobtitle:'Dev'} => null; {timeatjob:'none'} => 'You must select time at job.'; {timeatjob:'lessthanayear', othertimeatjob:'', jobtitle:'Dev'} => 'You must enter months at job.'

    validateForm91: {timeatjobcosigner:'over2years', jobtitlecosigner:'Dev'} => null; {timeatjobcosigner:'none'} => 'You must select time at job.'

    validateForm11: {monthlyincome:'3000'} => null; {} => 'Select your monthly income.'

    validateForm111: () => null (D-02: one test asserting returns null)

    validateForm13: {downpayment:'1000'} => null; {downpayment:'', otherdownpayment:''} => 'Enter a down payment amount.' Note: {downpayment:''} is falsy but otherdownpayment present => null

    validateForm14: {} => null (no fields = no error); {monthlydebt:'abc'} => 'You must enter a number.'; {monthlyincome:'xyz'} => 'You must enter a number.'

    validateForm15: {} => null (isRepoVisible falsy); {isRepoVisible:true, howlongsincerepo:''} => 'You must enter years/months since your repo'

    validateForm15cosigner: {} => null; {isRepoVisible:true, howlongsincerepocosigner:''} => 'You must enter years/months since your cosigner repo'

    validateForm17: {zipcode:'90210'} => null; {} => 'You must enter your zip code.'

    validateForm19: {firstname:'A',lastname:'B',email:'c@d.com',address:'123',city:'LA',state:'CA'} => null; {} => 'Enter Your First Name' (first missing field)

    validateForm20: {agreeauthorizedconsent:true, agreenda:true} => null; {} => 'You must agree to authorized consent.'

    signupDemoForm: {dealershipname:'D',firstname:'F',lastname:'L',email:'e@e.com',phone:'123'} => null; {} => 'You must enter the name.'

    validateFormHomepage: {loantype:'auto',zipcode:'90210'} => null; {} => 'You must select loan type.'; {loantype:'select'} => 'You must select loan type.'

    updateItem (D-03): call updateItem(5), assert return === 'Lorem ipsum', assert sessionStorage.getItem('item[5]') === 'Lorem ipsum'. Add sessionStorage.clear() in a beforeEach within the updateItem describe block.

    updateAmount (D-04): updateAmount(42) => 42; updateAmount('hello') => 'hello'
  </action>
  <verify>
    <automated>cd /Users/forumgandhi/Desktop/atoloan-ui && npx vitest run tests/unit/validators.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "validateForm5\|validateForm9\|validateForm91\|validateForm11\|validateForm111\|validateForm13\|validateForm14\|validateForm15\|validateForm15cosigner\|validateForm17\|validateForm19\|validateForm20\|signupDemoForm\|validateFormHomepage\|updateItem\|updateAmount" tests/unit/validators.test.js returns >= 16 (all 16 exports referenced)
    - grep "validateForm111" tests/unit/validators.test.js finds at least 1 match (D-02)
    - grep "updateItem" tests/unit/validators.test.js finds at least 2 matches (import + test) (D-03)
    - grep "updateAmount" tests/unit/validators.test.js finds at least 2 matches (D-04)
    - grep "sessionStorage" tests/unit/validators.test.js finds at least 1 match (D-03)
    - grep "Lorem ipsum" tests/unit/validators.test.js finds at least 1 match (D-03)
    - npx vitest run tests/unit/validators.test.js exits 0
  </acceptance_criteria>
  <done>
    - All 16 validator exports have at least one valid-input and one invalid-input test case
    - validateForm111 test asserts it returns null (D-02)
    - updateItem test confirms sessionStorage write+read returns 'Lorem ipsum' (D-03)
    - updateAmount test confirms passthrough behavior (D-04)
    - All tests green under npx vitest run
  </done>
</task>

</tasks>

<verification>
Run the full utility test suite:
```bash
cd /Users/forumgandhi/Desktop/atoloan-ui && npx vitest run tests/unit/loanCalculator.test.js tests/unit/payloads.test.js tests/unit/validators.test.js
```
All tests pass. No vitest warnings about act() or missing imports.
</verification>

<success_criteria>
- 3 test files exist in tests/unit/
- All tests pass with `npm run test:unit`
- loanCalculator tests use toBeCloseTo(expected, 2) for float values and toBe for months
- payloads tests verify trimming and shallow copy
- validators tests cover all 16 exports including validateForm111, updateItem, updateAmount
</success_criteria>

<output>
After completion, create `.planning/phases/02-unit-tests/02-01-SUMMARY.md`
</output>
