---
phase: 03-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/mocks/api.js
  - tests/e2e/fixtures/sample.pdf
  - tests/e2e/loan-wizard.spec.js
autonomous: true
requirements: [E2E-01, E2E-02, E2E-03, E2E-04]
must_haves:
  truths:
    - "Full happy-path loan wizard completes from loan-type to upload-documents step without errors"
    - "Invalid zip code triggers a dialog alert and does not advance past the zip-code step"
    - "Submitting contact form with missing fields shows 'Fix the following:' error text and does not advance"
    - "Selecting 'By Myself' on cosigner step skips cosigner-relative and same-address, lands on employment step"
  artifacts:
    - path: "tests/e2e/loan-wizard.spec.js"
      provides: "4 test blocks for E2E-01 through E2E-04"
      contains: "test('happy path"
    - path: "tests/e2e/mocks/api.js"
      provides: "Fixed /findback and /credit-unions mock responses"
      contains: "Test Credit Union"
    - path: "tests/e2e/fixtures/sample.pdf"
      provides: "Minimal valid PDF for file upload tests"
  key_links:
    - from: "tests/e2e/loan-wizard.spec.js"
      to: "tests/e2e/mocks/api.js"
      via: "import { registerAll }"
      pattern: "import.*registerAll.*from.*mocks/api"
    - from: "tests/e2e/loan-wizard.spec.js"
      to: "/loans route"
      via: "page.goto('/loans')"
      pattern: "page\\.goto.*loans"
---

<objective>
Fix API mock responses and write the loan wizard E2E spec covering all 4 wizard test scenarios.

Purpose: The loan wizard is the most complex user flow. E2E tests catch regressions in the 19-step wizard navigation, validation guards, and cosigner skip branch that unit tests cannot cover.

Output: Fixed `tests/e2e/mocks/api.js` with correct /findback and /credit-unions shapes, `tests/e2e/fixtures/sample.pdf` for upload tests, and `tests/e2e/loan-wizard.spec.js` with 4 test blocks.
</objective>

<execution_context>
@/Users/forumgandhi/Desktop/atoloan-ui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forumgandhi/Desktop/atoloan-ui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-e2e-tests/03-CONTEXT.md
@.planning/phases/03-e2e-tests/03-RESEARCH.md

<interfaces>
<!-- From tests/e2e/mocks/api.js -->
export async function registerAll(page) { ... }
// Registers page.route() interceptors for all 9 API endpoints
// Must be called before page.goto() in every test

<!-- From tests/e2e/health.spec.js — pattern to follow -->
import { test, expect } from '@playwright/test'
// test('description', async ({ page }) => { ... })

<!-- From playwright.config.js -->
// testDir: './tests/e2e', fullyParallel: true, baseURL: 'http://localhost:5173'
// webServer: { command: 'npm run dev', reuseExistingServer: true }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix API mocks and create PDF fixture</name>
  <files>tests/e2e/mocks/api.js, tests/e2e/fixtures/sample.pdf</files>
  <read_first>
    - tests/e2e/mocks/api.js (current mock file — see lines 30-35 for /findback and lines 74-89 for /credit-unions)
    - src/pages/ratesheet/useRateSheetUploader.js (line ~45: reads `data.credit_unions` — confirms required shape)
    - src/pages/loans/steps/DocumentUpload.jsx (reads `findBankResponse?.data` — confirms what /findback response shape the UI needs)
  </read_first>
  <action>
**Update tests/e2e/mocks/api.js — two changes (per D-02 and research Pitfall 3):**

1. **Fix /findback mock (line 30-35):** Change the response body from `{ banks: [] }` to:
```javascript
body: JSON.stringify({ banks: [{ name: 'Test Credit Union' }] })
```
This ensures E2E-01 happy path can assert the wizard reached the upload-documents step after /findback returns a non-empty result.

2. **Fix /credit-unions GET mock (line 74-89):** Change the response body from a plain array `[{ id: 1, name: 'Test Credit Union' }]` to the wrapped shape the hook expects:
```javascript
body: JSON.stringify({ credit_unions: [{ id: 1, name: 'Test Credit Union' }] })
```
The `useRateSheetUploader.js` hook reads `data.credit_unions` (not the top-level array), so the plain array causes banksList to be empty.

**Create tests/e2e/fixtures/sample.pdf:**

Create the `tests/e2e/fixtures/` directory and write a minimal valid PDF file. The file must have a valid `%PDF-1.` header so Playwright assigns `application/pdf` MIME type based on extension. Content is irrelevant since `/ratesheetuploader` is mocked (per D-05). Use this minimal PDF content:

```
%PDF-1.0
1 0 obj<</Type /Catalog /Pages 2 0 R>>endobj
2 0 obj<</Type /Pages /Kids[3 0 R] /Count 1>>endobj
3 0 obj<</Type /Page /MediaBox[0 0 3 3]>>endobj
xref
0 4
0000000000 65535 f 
0000000009 00000 n 
0000000058 00000 n 
0000000115 00000 n 
trailer<</Size 4 /Root 1 0 R>>
startxref
190
%%EOF
```
  </action>
  <verify>
    <automated>grep -c "Test Credit Union" tests/e2e/mocks/api.js && grep -c "credit_unions" tests/e2e/mocks/api.js && test -f tests/e2e/fixtures/sample.pdf && head -c 5 tests/e2e/fixtures/sample.pdf | grep -c '%PDF'</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/mocks/api.js /findback route contains `{ banks: [{ name: 'Test Credit Union' }] }`
    - tests/e2e/mocks/api.js /credit-unions GET route contains `{ credit_unions: [{ id: 1, name: 'Test Credit Union' }] }` (NOT a plain array)
    - tests/e2e/fixtures/sample.pdf exists and starts with `%PDF-1.`
    - tests/e2e/fixtures/ directory exists
  </acceptance_criteria>
  <done>Both mock responses corrected, PDF fixture created</done>
</task>

<task type="auto">
  <name>Task 2: Write loan-wizard.spec.js with 4 test blocks</name>
  <files>tests/e2e/loan-wizard.spec.js</files>
  <read_first>
    - tests/e2e/mocks/api.js (the updated mock file from Task 1 — to understand available mock responses)
    - tests/e2e/health.spec.js (existing spec pattern — import style, test structure)
    - src/pages/loans/loanSteps.js (step definitions — derive exact button labels and step titles)
    - src/pages/loans/useLoanWizard.js (wizard state machine — understand cosigner skip branch at 'by-myself', handleZipCodeContinue alert, handleContactInfoContinue errors)
    - src/pages/loans/steps/ZipCodeInput.jsx (zip input UI and error rendering)
    - src/pages/loans/steps/ContactInfoForm.jsx (8 required fields, error display with 'Fix the following:')
    - src/pages/loans/steps/DocumentUpload.jsx (final step after /findback — renders 'Upload Required Documents' heading)
  </read_first>
  <action>
**Create `tests/e2e/loan-wizard.spec.js` with 4 test blocks per D-03. Use ESM imports, `registerAll(page)` before every `page.goto('/loans')`.**

**All 4 tests share this preamble:**
```javascript
import { test, expect } from '@playwright/test'
import { registerAll } from './mocks/api.js'
```

**Test 1: E2E-01 Happy path (per D-01 — assert key milestones only, NOT every step title)**

Flow sequence — click through each step using these exact selectors:
1. `await page.goto('/loans')`
2. Assert milestone: `await expect(page.getByRole('heading', { name: 'Select Loan Type' })).toBeVisible()`
3. `await page.getByText('Auto Loans').click()` — loan-type
4. `await page.getByText('With Co-Signer').click()` — cosigner
5. `await page.getByText('Relative').click()` — cosigner-relative
6. `await page.getByText('Lives At Same Address').click()` — same-address
7. `await page.getByText('Full Time').click()` — employment
8. `await page.getByText('Paystub').click()` — proof-of-income
9. `await page.getByText('$3,000 Monthly').click()` — monthly-income
10. `await page.getByText('Over 2 Years').click()` — time-at-job (selects option)
11. `await page.getByPlaceholder("What's your job title?").fill('Engineer')`
12. `await page.getByRole('button', { name: 'Continue' }).click()` — time-at-job continue
13. `await page.getByText('Yes').first().click()` — drivers-license
14. `await page.getByText('$0').click()` — down-payment
15. `await page.getByText('Social Security').click()` — ssn-itin
16. `await page.getByRole('button', { name: 'Skip' }).click()` — dti skip
17. `await page.getByText('No').first().click()` — repo-history
18. `await page.getByText('Over 1 Year').click()` — residence-time
19. Assert milestone: zip-code step heading `'Your Zip Code For Current Residence?'`
20. `await page.getByPlaceholder('Enter your zip code').fill('90001')`
21. `await page.getByRole('button', { name: 'Continue' }).click()` — zip continue
22. `await page.getByText('Continue').first().click()` — approval-info continue option
23. Assert milestone: contact-info step heading `'Provide Your Contact Information'`
24. Fill contact form (city is auto-populated as 'Los Angeles' from zip mock — fill remaining 7 fields):
    - `page.getByPlaceholder('First Name').fill('John')`
    - `page.getByPlaceholder('Last Name').fill('Doe')`
    - `page.getByPlaceholder('Email Address').fill('john@example.com')`
    - `page.getByPlaceholder('Home Address').fill('123 Main St')`
    - `page.getByPlaceholder('State').fill('CA')`
    - `page.getByPlaceholder('Zip Code').fill('90001')`
    - `page.getByPlaceholder('Phone Number').fill('5551234567')`
25. `await page.getByRole('button', { name: 'Continue' }).click()` — contact continue
26. Assert milestone: review step heading `'Review & Continue'`
27. `await page.getByText('Find Bank').click()` — triggers /echo + /findback
28. Assert final milestone: `await expect(page.getByRole('heading', { name: 'Upload Required Documents' })).toBeVisible()`

**Test 2: E2E-02 Invalid zip code**

1. Navigate to `/loans`, click through to zip-code step (reuse steps 1-18 from happy path)
2. Before filling zip, override the /validate-zipcode mock to return invalid:
```javascript
await page.route('**/validate-zipcode', (route) =>
  route.fulfill({
    status: 200,
    contentType: 'application/json',
    body: JSON.stringify({ valid: false, message: 'Please enter a valid zip code.' }),
  })
)
```
3. Register dialog handler BEFORE clicking continue:
```javascript
let dialogMessage = ''
page.on('dialog', async (dialog) => {
  dialogMessage = dialog.message()
  await dialog.accept()
})
```
4. Fill zip `'00000'` and click Continue
5. Assert: `expect(dialogMessage).toContain('valid zip code')`
6. Assert: step heading still shows `'Your Zip Code For Current Residence?'` (did not advance)

**Test 3: E2E-03 Contact form validation errors**

1. Navigate to `/loans`, click through to contact-info step (steps 1-22 from happy path)
2. Click Continue without filling any fields
3. Assert: `await expect(page.getByText('Fix the following:')).toBeVisible()`
4. Assert: step heading still shows `'Provide Your Contact Information'` (did not advance)

**Test 4: E2E-04 Cosigner skip**

1. Navigate to `/loans`
2. `await page.getByText('Auto Loans').click()` — loan-type
3. `await page.getByText('By Myself').click()` — cosigner (skip branch)
4. Assert: `await expect(page.getByRole('heading', { name: 'Select Employment Type' })).toBeVisible()` — landed on employment step, skipped cosigner-relative and same-address

**Important implementation notes:**
- Use `page.getByText()` for image-option buttons (images 404 in test env, text fallback renders). Do NOT use `page.getByRole('button', { name })` for image buttons.
- Use `page.getByRole('button', { name: 'Continue' })` ONLY for non-image buttons (ZipCodeInput, ContactInfoForm, TimeAtJobInput) where only one Continue button renders at a time.
- For the approval-info step, use `page.getByText('Continue').first()` since the option label is "Continue" rendered as text fallback.
- Do NOT assert every step title per D-01. Only assert the 5 milestones: step 1 loads, zip step reached, contact step reached, review step reached, upload-documents step reached.
- Each test must call `await registerAll(page)` before `page.goto('/loans')`.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/loan-wizard.spec.js --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/loan-wizard.spec.js exists and contains `import { registerAll } from './mocks/api.js'`
    - File contains exactly 4 `test(` calls
    - Test 1 contains `page.getByText('Auto Loans').click()` and asserts heading 'Upload Required Documents'
    - Test 2 contains `page.on('dialog'` and asserts `dialogMessage` contains 'valid zip code'
    - Test 3 contains assertion for `'Fix the following:'` text
    - Test 4 contains `page.getByText('By Myself').click()` and asserts heading 'Select Employment Type'
    - `npx playwright test tests/e2e/loan-wizard.spec.js` exits with 0 (all 4 tests pass)
  </acceptance_criteria>
  <done>All 4 loan wizard E2E tests pass: happy path completes to upload-documents, invalid zip shows dialog error, empty contact form shows validation errors, cosigner skip lands on employment</done>
</task>

</tasks>

<verification>
```bash
npx playwright test tests/e2e/loan-wizard.spec.js --reporter=list
```
All 4 tests pass. No source files modified (per D-07).
</verification>

<success_criteria>
- tests/e2e/loan-wizard.spec.js has 4 passing tests covering E2E-01 through E2E-04
- tests/e2e/mocks/api.js has corrected /findback and /credit-unions mock shapes
- tests/e2e/fixtures/sample.pdf exists as a valid PDF file
- `npx playwright test tests/e2e/loan-wizard.spec.js` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-e2e-tests/03-01-SUMMARY.md`
</output>
