---
phase: 03-e2e-tests
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - tests/e2e/rate-sheet-uploader.spec.js
  - tests/e2e/loan-calculator.spec.js
autonomous: true
requirements: [E2E-05, E2E-06, E2E-07, E2E-08]
must_haves:
  truths:
    - "PDF upload triggers section review showing 'Review 1 of 7' heading"
    - "Confirming all 7 sections shows 'All sections confirmed!' and Save to Database button"
    - "View existing mode loads bank list and selecting a bank loads its rate sheet"
    - "Loan calculator renders monthly payment value and pie chart after clicking Calculate"
  artifacts:
    - path: "tests/e2e/rate-sheet-uploader.spec.js"
      provides: "3 test blocks for E2E-05, E2E-06, E2E-07"
      contains: "test('upload"
    - path: "tests/e2e/loan-calculator.spec.js"
      provides: "1 test block for E2E-08"
      contains: "test('calculate"
  key_links:
    - from: "tests/e2e/rate-sheet-uploader.spec.js"
      to: "tests/e2e/mocks/api.js"
      via: "import { registerAll }"
      pattern: "import.*registerAll.*from.*mocks/api"
    - from: "tests/e2e/rate-sheet-uploader.spec.js"
      to: "tests/e2e/fixtures/sample.pdf"
      via: "page.setInputFiles"
      pattern: "setInputFiles.*sample\\.pdf"
    - from: "tests/e2e/loan-calculator.spec.js"
      to: "/loancalculator route"
      via: "page.goto('/loancalculator')"
      pattern: "page\\.goto.*loancalculator"
---

<objective>
Write E2E specs for the rate sheet uploader (upload, review, view-existing) and the loan calculator.

Purpose: These specs cover the second and third most complex user flows. The rate sheet uploader has a multi-step review with 7 sections and two distinct modes. The loan calculator verifies computation output renders correctly.

Output: `tests/e2e/rate-sheet-uploader.spec.js` with 3 test blocks and `tests/e2e/loan-calculator.spec.js` with 1 test block.
</objective>

<execution_context>
@/Users/forumgandhi/Desktop/atoloan-ui/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forumgandhi/Desktop/atoloan-ui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-e2e-tests/03-CONTEXT.md
@.planning/phases/03-e2e-tests/03-RESEARCH.md
@.planning/phases/03-e2e-tests/03-01-SUMMARY.md

<interfaces>
<!-- From tests/e2e/mocks/api.js (after Plan 01 fixes) -->
export async function registerAll(page) { ... }
// POST /ratesheetuploader → returns 7-section JSON (all empty objects)
// POST /update → returns { success: true }
// GET /credit-unions → returns { credit_unions: [{ id: 1, name: 'Test Credit Union' }] }
// GET /credit-unions/*/ratesheet → returns 7-section JSON

<!-- From src/pages/ratesheet/constants.js -->
export const reviewOrder = [
  'credit_union_info', 'rate_policy', 'loan_programs', 'guidelines',
  'special_programs', 'participation_and_funding', 'additional_details'
]
// toLabel(key) converts underscores to spaces, lowercase

<!-- From src/pages/LoanCalculator.jsx -->
// Default values: vehiclePrice=35000, downPayment=7000, loanTerm=72, interestRate=11.33
// Labels: "Vehicle Price", "Down Payment", "Loan Term Months", "Interest Rate"
// Button: "Calculate" (id="calculate")
// Result text: "Monthly Payment is:" with value "$XXX.XX / Month for N Years"
// Chart: <canvas id="loanChart">
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write rate-sheet-uploader.spec.js with 3 test blocks</name>
  <files>tests/e2e/rate-sheet-uploader.spec.js</files>
  <read_first>
    - tests/e2e/mocks/api.js (updated mock file — understand /ratesheetuploader, /update, /credit-unions, /credit-unions/*/ratesheet responses)
    - tests/e2e/fixtures/sample.pdf (confirm it exists from Plan 01)
    - src/pages/ratesheet/useRateSheetUploader.js (state machine — understand upload flow, viewMode states, section review logic, view-existing flow)
    - src/pages/ratesheet/constants.js (reviewOrder array, toLabel function, copyByLanguage)
    - src/pages/ratesheet/RateSheetUploader.jsx (or equivalent — understand rendered selectors: ModeToggle, file input, upload button, section review headings)
    - tests/e2e/health.spec.js (existing spec pattern)
  </read_first>
  <action>
**Create `tests/e2e/rate-sheet-uploader.spec.js` with 3 test blocks. ESM imports, `registerAll(page)` before every `page.goto('/ratesheetuploader')`.**

```javascript
import { test, expect } from '@playwright/test'
import { registerAll } from './mocks/api.js'
import path from 'path'
```

**Test 1: E2E-05 PDF upload flow**

1. `await registerAll(page)`
2. `await page.goto('/ratesheetuploader')`
3. Assert page heading: `await expect(page.getByRole('heading', { name: 'Upload Ratesheet' })).toBeVisible()`
4. Set file on input using `page.setInputFiles()`:
```javascript
const pdfPath = path.resolve('tests/e2e/fixtures/sample.pdf')
await page.getByLabel('Choose PDF file').setInputFiles(pdfPath)
```
5. Click upload: `await page.getByRole('button', { name: 'Upload Ratesheet' }).click()`
6. Wait for section review to appear: `await expect(page.getByText('Review 1 of 7')).toBeVisible()`
7. Assert first section label visible: `await expect(page.getByText('credit union info')).toBeVisible()`

**Test 2: E2E-06 Section review through all 7 sections to Save**

1. `await registerAll(page)`, goto `/ratesheetuploader`
2. Upload PDF (same steps as Test 1: setInputFiles + click Upload)
3. Wait for `'Review 1 of 7'` to be visible
4. Click "Confirm & Next" for sections 1-6:
```javascript
for (let i = 1; i <= 6; i++) {
  await expect(page.getByText(`Review ${i} of 7`)).toBeVisible()
  await page.getByRole('button', { name: 'Confirm & Next' }).click()
}
```
5. Section 7: `await expect(page.getByText('Review 7 of 7')).toBeVisible()`
6. Click Confirm (NOT "Confirm & Next"): `await page.getByRole('button', { name: 'Confirm' }).click()`
   NOTE: The "Confirm" button for section 7 must be distinguished from "Confirm & Next". Use exact match or `.filter()` if needed. The button text for section 7 is exactly "Confirm" without "& Next".
7. Assert: `await expect(page.getByText('All sections confirmed!')).toBeVisible()`
8. Click Save: `await page.getByRole('button', { name: /Save to Database/ }).click()` (use regex to handle emoji prefix)
9. Assert save success: `await expect(page.getByText('Successfully saved to database!')).toBeVisible()`

**Test 3: E2E-07 View existing mode**

1. `await registerAll(page)`, goto `/ratesheetuploader`
2. Switch to view-existing mode: `await page.getByText('View Existing Rate Sheet').click()`
3. Wait for bank selector: `await expect(page.getByLabel('Select Credit Union / Bank:')).toBeVisible()`
4. Select bank from dropdown:
```javascript
await page.getByLabel('Select Credit Union / Bank:').selectOption({ label: 'Test Credit Union' })
```
5. Assert rate sheet loaded: `await expect(page.getByText('Rate sheet loaded successfully')).toBeVisible()`

**Important implementation notes:**
- The file input has `aria-label="Choose PDF file"` (from `rs.copy.selectFile` in EN). Use `page.getByLabel('Choose PDF file')`.
- For section 7 "Confirm" button: use `page.getByRole('button', { name: 'Confirm', exact: true })` to avoid matching "Confirm & Next".
- The Save button renders with emoji prefix: `'Save to Database'`. Use regex: `{ name: /Save to Database/ }`.
- The `/credit-unions` mock now returns `{ credit_unions: [...] }` (fixed in Plan 01), so the bank selector dropdown will populate correctly.
- Use `path.resolve('tests/e2e/fixtures/sample.pdf')` for the fixture path — works from the project root where Playwright runs.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/rate-sheet-uploader.spec.js --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/rate-sheet-uploader.spec.js exists and contains `import { registerAll } from './mocks/api.js'`
    - File contains exactly 3 `test(` calls
    - Test 1 contains `setInputFiles` with `sample.pdf` and asserts `'Review 1 of 7'`
    - Test 2 contains a loop clicking `'Confirm & Next'` 6 times, then `'Confirm'` once, then asserts `'All sections confirmed!'` and `'Successfully saved to database!'`
    - Test 3 contains `selectOption` with `'Test Credit Union'` and asserts `'Rate sheet loaded successfully'`
    - `npx playwright test tests/e2e/rate-sheet-uploader.spec.js` exits with 0 (all 3 tests pass)
  </acceptance_criteria>
  <done>All 3 rate sheet uploader E2E tests pass: upload shows section review, 7-section confirm reaches save, view-existing loads bank rate sheet</done>
</task>

<task type="auto">
  <name>Task 2: Write loan-calculator.spec.js</name>
  <files>tests/e2e/loan-calculator.spec.js</files>
  <read_first>
    - src/pages/LoanCalculator.jsx (full component — understand input labels, Calculate button, result rendering, canvas element)
    - tests/e2e/health.spec.js (existing spec pattern)
  </read_first>
  <action>
**Create `tests/e2e/loan-calculator.spec.js` with 1 test block for E2E-08.**

```javascript
import { test, expect } from '@playwright/test'
```

**Test 1: E2E-08 Calculate payment and chart renders**

1. `await page.goto('/loancalculator')`
2. Assert page heading: `await expect(page.getByRole('heading', { name: 'Auto Loan Calculator' })).toBeVisible()`
3. The calculator has default values (vehiclePrice=35000, downPayment=7000, loanTerm=72, interestRate=11.33), so clicking Calculate without changing inputs is valid.
4. Click Calculate: `await page.getByRole('button', { name: 'Calculate' }).click()`
5. Assert monthly payment value is no longer `---`:
```javascript
// The result shows "$XXX.XX / Month for N Years" — assert the dollar value is not '---'
await expect(page.locator('#paymentResults')).toContainText(/\$[\d,]+\.\d{2}/)
```
6. Assert "Monthly Payment is:" is visible: `await expect(page.getByText('Monthly Payment is:')).toBeVisible()`
7. Assert "Total Interest Paid:" is visible with a value: `await expect(page.getByText('Total Interest Paid:')).toBeVisible()`
8. Assert the pie chart canvas is visible: `await expect(page.locator('#loanChart')).toBeVisible()`

**Note:** No `registerAll(page)` needed — the calculator page makes no API calls. It's a pure client-side computation.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/loan-calculator.spec.js --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/loan-calculator.spec.js exists
    - File contains 1 `test(` call
    - Test navigates to `/loancalculator`, clicks `'Calculate'` button, asserts `#paymentResults` contains a dollar amount regex `\$[\d,]+\.\d{2}`, and asserts `#loanChart` canvas is visible
    - `npx playwright test tests/e2e/loan-calculator.spec.js` exits with 0
  </acceptance_criteria>
  <done>Loan calculator E2E test passes: Calculate click renders monthly payment value and pie chart</done>
</task>

</tasks>

<verification>
```bash
npx playwright test tests/e2e/rate-sheet-uploader.spec.js tests/e2e/loan-calculator.spec.js --reporter=list
```
All 4 tests pass (3 rate sheet + 1 calculator).
</verification>

<success_criteria>
- tests/e2e/rate-sheet-uploader.spec.js has 3 passing tests covering E2E-05, E2E-06, E2E-07
- tests/e2e/loan-calculator.spec.js has 1 passing test covering E2E-08
- `npx playwright test tests/e2e/rate-sheet-uploader.spec.js tests/e2e/loan-calculator.spec.js` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-e2e-tests/03-02-SUMMARY.md`
</output>
